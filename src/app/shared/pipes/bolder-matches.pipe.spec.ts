import { BolderMatchesPipe } from './bolder-matches.pipe';

describe('BolderMatchesPipe', () => {
  it('create an instance', () => {
    const pipe = new BolderMatchesPipe();
    expect(pipe).toBeTruthy();
  });
});
